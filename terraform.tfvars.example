# Example Terraform variables file
# Copy this file to terraform.tfvars and fill in the values

# GitHub Organization
github_organization = "your-github-org"

# GitHub App Authentication
# Create a GitHub App and get these values from the app settings
github_app_id              = "123456"
github_app_installation_id = "123456789"
github_app_pem_file        = "path/to/your/github-app-private-key.pem"

# Parent Team Configuration
parent_team_name = "canary-trips"

# Platform Team Members (GitHub usernames)
platform_team_maintainers = [
  "platform-lead",
  "infra-admin"
]

platform_team_members = [
  "platform-engineer-1",
  "platform-engineer-2"
]

# Template Approvers Team Members
template_approvers_maintainers = [
  "template-approver-lead",
  "senior-developer"
]

template_approvers_members = [
  "template-reviewer-1",
  "template-reviewer-2"
]

# Security Team Members
security_team_members = [
  "security-engineer-1",
  "security-lead"
]

# Read-only Team Members
read_only_team_members = [
  "auditor-1",
  "stakeholder-1"
]

# Developers Team Members
developers_team_maintainers = [
  "dev-lead",
  "tech-lead"
]

developers_team_members = [
  "developer-1",
  "developer-2"
]

# Branch Protection Settings
required_status_checks = [
  "ci-template",
  "lint",
  "docs-build",
  "codeql"
]

required_pull_request_reviews = 1

# Template Repositories Configuration
# You can customize this if you need different repositories or settings
# template_repositories = {
#   "backstage-template-custom-service" = {
#     description = "Custom Backstage template for special services"
#     topics      = ["backstage", "template", "custom", "service"]
#   }
# }

# Backstage IDP Repository Configuration
# You can customize the main Backstage repository settings
# backstage_repository = {
#   name        = "backstage"
#   description = "Organization's Internal Developer Platform (IDP) powered by Backstage"
#   topics      = ["backstage", "idp", "developer-platform", "portal"]
# }

# Organization Security Settings
# Configure GitHub Advanced Security defaults for new repositories
github_organization_billing_email = "billing@your-github-org.com"

# GitHub Advanced Security settings for new repositories (defaults to true)
# advanced_security_enabled_for_new_repositories = true
# dependabot_alerts_enabled_for_new_repositories = true
# dependabot_security_updates_enabled_for_new_repositories = true
# dependency_graph_enabled_for_new_repositories = true
# secret_scanning_enabled_for_new_repositories = true
# secret_scanning_push_protection_enabled_for_new_repositories = true

# Custom Properties Configuration
# Enable or disable custom properties management
enable_custom_properties = true

# You can customize the organization-level custom properties
# organization_custom_properties = {
#   "service-tier" = {
#     description    = "Service tier classification for operational support"
#     property_type  = "single_select"
#     required       = true
#     allowed_values = ["tier-1", "tier-2", "tier-3", "experimental"]
#   }
#   "team-owner" = {
#     description   = "Team responsible for maintaining this repository"
#     property_type = "string"
#     required      = true
#   }
#   "environment" = {
#     description    = "Environment classification"
#     property_type  = "single_select"
#     required       = false
#     allowed_values = ["production", "staging", "development"]
#   }
# }

# Customize default property values for template repositories
# template_repository_custom_properties = {
#   "backstage-template-node-service" = {
#     service_tier = "tier-3"
#     team_owner   = "platform-team"
#   }
# }
