############################################################
# Example environment file for this template
# Copy to .env (never commit real secrets)
############################################################

# Application
PORT=3000
NODE_ENV=development

# Database (Dev Container Postgres service)
PGHOST=db
PGPORT=5432
PGDATABASE=app_db
PGUSER=app_user
PGPASSWORD=app_password

# Optional Observability / Tracing placeholders
# LOG_LEVEL=info
# OTEL_EXPORTER_OTLP_ENDPOINT=

# Security placeholders
# JWT_SECRET=change_me

# Feature flags
# FEATURE_X_ENABLED=false