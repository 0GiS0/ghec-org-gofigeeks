--- rendered.yaml (steps excerpt) ---
68:   # here's the steps that are executed in series in the scaffolder backend
69:   # You can see all actions you have registered here:
70:   # http://localhost:3000/create/actions
71:   steps:
72:     - id: fetch-base
73:       name: Fetch Template
74:       action: fetch:template
75:       input:
76:         url: ./skeleton
77:         copyWithoutTemplating:
78:           - .github/workflows/*
79:         values:
80:           name: ${{ parameters.name }}
81:           owner: ${{ parameters.owner }}
82:           description: ${{ parameters.description }}
83:           destination: ${{ parameters.repoUrl | parseRepoUrl }}
84:           repoUrl: ${{ parameters.repoUrl }}
85:           system: ${{ parameters.system }}
86:     - id: publish
87:       name: Publish
88:       action: publish:github
89:       input:
90:         description: |-
91:           ${{ (() => {
92:             const raw = String(parameters.description ?? '');
93:             // Normalize whitespace/newlines to one line
94:             const oneLine = raw.replace(/\s+/g, ' ').trim();
95:             // Limit to 350 UTF-8 bytes (GitHub's actual limit)
96:             let truncated = oneLine;
97:             while (new TextEncoder().encode(truncated).length > 350) {
98:               truncated = truncated.slice(0, -1);
99:             }
100:             return truncated;
101:           })() }}
102:         repoUrl: ${{ parameters.repoUrl }}
103:         gitCommitMessage: Create scaffold from template
104:         topics: ["backstage-include", "GofiGeeksOrg"]
105:         defaultBranch: main
106:     - id: register
107:       name: Register
108:       action: catalog:register
109:       input:
110:         repoContentsUrl: ${{ steps['publish'].output.repoContentsUrl }}
\nYAML written to /home/runner/work/ghec-org-as-code/ghec-org-as-code/.local-validate/rendered.yaml
